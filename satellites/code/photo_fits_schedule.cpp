#ifndef photo_fits_schedule
#define photo_fits_schedule

#include "util.cpp"

using namespace std;

//can the satellite take a photo of the given object at the given time with
//regard to the photos the satellite already takes
bool photo_fits_schedule(Input& input, Satellite& satellite, Object& object, long long time) {
	//TODO fill me
}

#endif
